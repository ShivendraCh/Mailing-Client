class Configuration:
    def smtp_server_configuration(self):
        # SMTP Server Configuration
        HOST = "smtp.gmail.com"
        PORT = 587
        print(f"SMTP Server: {HOST}, Port: {PORT}")
        # Can add logic to edit or save these settings later
